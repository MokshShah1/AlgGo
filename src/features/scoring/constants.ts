/** XP and streak constants (PRD section 15). */
export const XP_PER_SOLVABLE = 5;
export const LESSON_COMPLETION_BONUS = 25;
export const MAX_STREAK_CHARGES = 2;

/** Daily goal: complete one lesson OR complete this many solvables. */
export const DAILY_GOAL_SOLVABLES = 3;

/** Lesson is "mastered" for slope-ratio when success rate meets this. */
export const MASTERY_SUCCESS_THRESHOLD = 0.8;
